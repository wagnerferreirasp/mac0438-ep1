#include <stdio.h>
#include <stdlib.h>


int main(int argc, char const *argv[])
{
	int m, n, r, s;
	m = atoi(argv[1]);
	n = atoi(argv[2]);
	r = atoi(argv[3]);
	s = atoi(argv[4]);
	printf("%d %d %d %d\n", m, n, r, s);

	return 0;
}